torchsde==0.2.6
einops==0.8.0
transformers==4.50.0
safetensors==0.4.3
accelerate==0.32.1
pyyaml==6.0.1
pillow==10.4.0
scipy==1.14.0
tqdm==4.66.4
psutil==6.0.0
omegaconf==2.3.0
# DO NOT BUMP gradio above 3.41.2 — this fork relies on the Gradio 3.x DOM
# structure for JS/DOM injection (Asset Browser icons, accordion tabs, custom
# header buttons). Gradio 4.x rewrites the event system and HTML output and
# would break every custom-* feature (8, 9, 10). Known Dependabot alerts for
# gradio 3.41.x (path traversal, SSRF, /file access) are NOT exploitable in
# the intended local/LAN deployment — see "Security note" in readme.md.
gradio==6.15.0
# DO NOT BUMP fastapi / starlette. Gradio 3.41.2 uses the legacy
# TemplateResponse(name, context) signature. starlette 1.0.0 (and fastapi
# >= 0.115 which transitively pulls it) removed that overload — calling it
# crashes with: TypeError: unhashable type: 'dict' in jinja2/utils.py,
# because the context dict ends up where starlette expects the request.
# fastapi 0.112.4 is the last release that pins starlette < 0.39.
fastapi==0.112.4
starlette==0.38.6
pygit2==1.15.1
opencv-contrib-python-headless==4.10.0.84
httpx==0.27.0
onnxruntime==1.18.1
timm==1.0.7
numpy==1.26.4
tokenizers==0.21.0
packaging==24.1
rembg==2.0.57
groundingdino-py==0.4.0
segment_anything==1.0
